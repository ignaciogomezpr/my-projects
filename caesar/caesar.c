#include <cs50.h>
#include <stdio.h>
#include <ctype.h>

int main(int argc, string argv[])
{
    if (argc < 3 && isalpha(argv[1]) = 0 && isalnum(argv[1]) != 0)
    {
        printf ("working");
    }

}
